PDF.js 6.3.289, from the official `pdfjs-dist@6.3.289` npm package.

https://github.com/mozilla/pdf.js

Only the display module, worker and Apache-2.0 LICENSE are bundled. No CDN requests are made at runtime. The source PDF embeds its fonts; WASM image acceleration is disabled to keep the offline bundle self-contained.
